[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "storage"
version = "0.1.0"
description = "Shared storage clients (S3, Redis)"
requires-python = ">=3.12"
dependencies = ["core", "boto3>=1.34", "botocore>=1.34", "redis>=5.0"]

[tool.uv.sources]
core = { workspace = true }
