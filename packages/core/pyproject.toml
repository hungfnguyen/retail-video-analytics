[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "core"
version = "0.1.0"
description = "Shared models, settings, constants"
requires-python = ">=3.12"
dependencies = []
